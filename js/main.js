// const images = document.querySelectorAll('.relative img');
// let index = 0;

// setInterval(() => {
//   images[index].classList.add('top-full');
//   index = (index + 1) % images.length;
//   images[index].classList.remove('top-full');
// }, 3000);